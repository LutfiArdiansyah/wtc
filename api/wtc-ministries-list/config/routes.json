{
  "routes": [
    {
      "method": "GET",
      "path": "/wtc-ministries-lists",
      "handler": "wtc-ministries-list.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/wtc-ministries-lists/count",
      "handler": "wtc-ministries-list.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/wtc-ministries-lists/:id",
      "handler": "wtc-ministries-list.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/wtc-ministries-lists",
      "handler": "wtc-ministries-list.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/wtc-ministries-lists/:id",
      "handler": "wtc-ministries-list.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/wtc-ministries-lists/:id",
      "handler": "wtc-ministries-list.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
