{
  "routes": [
    {
      "method": "GET",
      "path": "/wtc-mission",
      "handler": "wtc-mission.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/wtc-mission",
      "handler": "wtc-mission.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/wtc-mission",
      "handler": "wtc-mission.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
